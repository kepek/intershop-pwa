# CamfilIcons

This library was generated with [Angular CLI](https://github.com/angular/angular-cli) version 10.1.2.

## Code scaffolding

Run `ng generate component component-name --project camfil-icons` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module --project camfil-icons`.

> Note: Don't forget to add `--project camfil-icons` or else it will be added to the default project in your `angular.json` file.

## Build

Run `ng build camfil-icons` to build the project. The build artifacts will be stored in the `dist/` directory.

## Publishing

After building your library with `ng build camfil-icons`, go to the dist folder `cd dist/camfil-icons` and run `npm publish`.

## Running unit tests

Run `ng test camfil-icons` to execute the unit tests via [Jest](https://www.npmjs.com/package/@angular-builders/jest).

## Further help

To get more help on the Angular CLI use `ng help` or go check out the [Angular CLI README](https://github.com/angular/angular-cli/blob/master/README.md).
